import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.workya.app',
  appName: 'workya-app',
  webDir: 'dist/workya-app',
  bundledWebRuntime: false
};

export default config;
